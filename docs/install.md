# Installation

> **Note:** This file describes the legacy n8n/Docker architecture.
> The current Zikra server is a Python FastAPI app.
> See the [README](../README.md) for current installation instructions.

If you specifically need the n8n/Docker deployment, the configuration
files are available in the git history prior to the merge commit.
