# Direct runtime dependencies are pinned because the container is the release
# artifact. Update them deliberately and run the container migration rehearsal.
fastapi==0.140.7
uvicorn==0.51.0
httpx==0.28.1
sqlite-vec==0.1.9
aiosqlite==0.22.1
python-dotenv==1.2.2
mcp[cli]==1.28.1
starlette==1.3.1

# PostgreSQL mode is installed through the setup.py postgres extra.
